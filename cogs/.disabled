# add cogs by name to this folder
# any cogs what are named here will not load when the bot is started
# any line that starts with # will be ignored
# any line that isnt a cog wont throw errors

template.py